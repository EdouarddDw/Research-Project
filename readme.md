# Research-Project
All packages are in the requirements.txt
create a virutal enviorment using (mac):
`python3 -m venv .venv`. (mac)
`py -m venv .venv` (windows)

Then install all packages using:
`pip install -r requirements.txt`

